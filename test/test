type Color a
    = Red a
    | Blue
    | Green
    | RGB Int Int Int


foo : a -> b -> Int -> Color a
foo x n c =
    let
        bar =
            Green

        baz =
            bar

        blah =
            "yo\nyo\n"
    in
    if True then
        Red x

    else
        baz
