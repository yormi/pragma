type Color
    = Red
    | Blue
    | Green
    | RGB Int Int Int


b : Int
b =
    2


foo : (Int -> Int -> Bool) -> Int -> Int
foo f b =
    let
        c = \x -> x
    in
    if identity True then
        b
    else
        identity 3


identity : something -> something
identity x =
    x
