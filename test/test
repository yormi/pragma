biggerThan : Int -> Int -> Bool
biggerThan a c =
    True


b : Int
b =
    35


foo : (Int -> Int -> Bool) -> Int -> Int
foo f =
    let
        blah =
            if f 3 3 then
                4
            else
                3
    in
    blah

