biggerThan : Int -> Int -> Bool
biggerThan a c =
    True


b : Int
b =
    35


foo : (Int -> Int -> Bool) -> Int -> Int
foo f b =
    let
        c = \x -> x
    in
    if f 3 3 then
        c True
    else
        c 3

