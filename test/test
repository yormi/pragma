biggerThan : Int -> Int -> Bool
biggerThan a c =
    True


b : Int
b =
    35


foo : (Int -> Int -> Bool) -> Int -> Int
foo f a =
    let
        blah =
            if f a 3 then
                a
            else
                3.3
    in
    blah

